[project]
name = "recruitbd-ai"
version = "0.1.0"
description = "RecruitBD-AI is an AI-powered CV parsing and job matching platform."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "aiohttp>=3.14.3",
  "bs4>=0.0.2",
  "fastapi>=0.141.1",
  "google-genai>=2.17.0",
  "numpy>=2.5.2",
  "ollama>=0.6.2",
  "pdfplumber>=0.11.10",
  "pytest>=9.1.1",
  "python-docx>=1.2.0",
  "python-dotenv>=1.2.2",
  "python-multipart>=0.0.32",
  "sentence-transformers>=5.7.0",
  "tqdm>=4.70.0",
  "uvicorn>=0.52.1",
]

[project.optional-dependencies]
cpu = ["torch>=2.13.0"]
cuda = ["torch>=2.13.0"]

[dependency-groups]
dev = ["pre-commit>=4.6.2"]

[tool.pytest]
pythonpath = ["."]

[tool.ruff]
line-length = 120

[tool.uv]
conflicts = [[{ extra = "cpu" }, { extra = "cuda" }]]

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-cu130", extra = "cuda" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true
